{
	"name": "require-import-alias",
	"version": "1.0.8",
	"description": "A tool which could set an alias for the module's path which is required in the require of nodejs or import of ES6",
	"main": "index.js",
	"scripts": {
	  "t": "node test/test.js",
	  "test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": ["require", "import", "alias", "webpack", "es6"],
	"author": "Alex bonjs",
	"license": "ISC",
	"dependencies": {
	  "require-alias-node": "*"
	}
}
